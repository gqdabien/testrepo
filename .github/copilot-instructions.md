# Repository Guidance for AI Coding Agents

This repository is currently a minimal placeholder with a single `README.md` and no source code.
Use these instructions to understand the structure and to know what to ask the user.

## Big Picture
- **Structure**: only the root folder contains `README.md`; no `src/`, `tests/`, or configuration files exist.
- **Purpose**: appears to be an empty test repository; there is no application, service, or library to modify.
- **Architectural concepts**: none to analyze; if the user adds files later, inspect them for new patterns.

## Developer Workflows
- There are no build, test, or lint commands defined.  When asked, confirm with the user what build tooling they plan to use.
- `git` is the only external dependency as far as the repo structure is concerned.

## Conventions and Patterns
- No project-specific conventions currently exist.  Follow default GitHub repository norms if new code is added.
- If the repo expands, look for patterns in directory names like `src/`, `lib/`, `tests/`, etc.

## External Integrations
- None present.  No CI configurations, package manifests, or service connections exist.

## When Working with the User
- Ask clarifying questions about the intended language, framework, or architecture if they request new features.
- Since there is no existing code, prompt the user for requirements before generating substantial code.

> **Note:** This file will need updating once the repository gains content. Agents should review it again after any commits that introduce new components.

---

*This guidance was autogenerated based on the current contents of the repository.  Please let me know if there are additional tools, workflows, or conventions I should document.*